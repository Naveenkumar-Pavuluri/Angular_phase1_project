 import { AppComponent } from "./app.component";
describe('appcomponent',()=>{
    it('testing title for the project',()=>{
        const title = 'Phase1-endproject';
        expect(title).toEqual;
    })
})